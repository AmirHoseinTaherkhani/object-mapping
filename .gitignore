# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.env

# YOLOv8 runs and inference outputs
runs/
outputs/predictions/

# Model weights (large binaries)
models/weights/*.pt
cloud_weights/

# Training data and labeling (large, not for version control)
labeling_data/
datasets.zip

# Demo videos and generated outputs
Demo/
outputs/*.mp4
outputs/*.csv
outputs/*.avi
outputs/*.json
outputs/model_comparison/
src/outputs/

# Datasets (large)
newDatasets/

# Root-level loose model weights
/*.pt

# Counting experiment outputs (user-specific, not for version control)
counting_experiment/output_counted.mp4
counting_experiment/roi.json

# Real-time inference outputs and exported model formats
realtime_inference/output_coreml.mp4
realtime_inference/output_tensorrt.mp4
models/weights/*.mlpackage
models/weights/*.engine

# MLflow
mlruns/
mlflow.db

# Notebooks
notebooks/

# Large files (but allow .dvc files)
/Data
/weights
/train3

# DVC - always allow these
!*.dvc
!.dvc/

# Claude internal (settings are local; CLAUDE.md is project docs and is tracked)
.claude/*
!.claude/CLAUDE.md

# Misc
.DS_Store
*.log

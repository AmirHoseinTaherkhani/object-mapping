# Core dependencies for running the counting pipelines locally.
# Covers realtime_inference/ and counting_experiment/.
#
# For the webapp and FastAPI: pip install -r requirements/prod.txt
# For Docker reproducible builds: requirements/docker.txt (pinned, headless OpenCV)
# For tests: pip install -r requirements/test.txt

ultralytics>=8.4.0
boxmot>=10.0.0
opencv-python>=4.8.0
numpy>=1.24.0
matplotlib>=3.7.0
pillow>=10.0.0
pyyaml>=6.0
tqdm>=4.60.0
scipy>=1.9.0
seaborn>=0.13.0
pandas>=2.0.0
psutil>=5.9.0
